##
## Makefile generated by Times tool
##

LEGOS_ROOT=../
# name for the legOS kernel
KERNEL=$(LEGOS_ROOT)/boot/legOS

PROGRAMS=periodic_sporadic.lx
# extra dynamic sources
DOBJECTS=brickos_kernel.o periodic_sporadic_init.o 
MAKESIG=28E0


#SYSHDR=periodic_sporadic.h

# Generated files. Will be removed by make realclean
GENFILES=periodic_sporadic_init.h periodic_sporadic_init.c periodic_sporadic_global.h periodic_sporadic.h periodic_sporadic.c

# Command line flag to gcc
CMACROS=-imacros periodic_sporadic.h

all: $(PROGRAMS)

include $(LEGOS_ROOT)/Makefile.common
include $(LEGOS_ROOT)/Makefile.user

# doc/html subdirectory

c++:
	$(MAKE) -C c++ all

html:
	doxygen Doxyfile

depend:
	$(MAKEDEPEND) *.c > .depend

.depend:
	$(MAKEDEPEND) *.c > .depend

tag:
	ctags --format=1 *.c include/*.h include/*/*.h *.c

clean:
	rm -rf .depend* *.o *.dis *.dis2 *~ *.bak *.tgz *.s tags *.ds1 *.ds2 *.dmap *.dcoff *.srec *.map *.coff

realclean: clean
	rm -f *.lx brickos_* $(GENFILES)

